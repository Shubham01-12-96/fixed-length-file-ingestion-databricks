# fixed-length-file-ingestion-databricks
Databricks-based PySpark project to ingest and process fixed-length files using schema-driven parsing, transforming legacy data into structured, analytics-ready datasets.
